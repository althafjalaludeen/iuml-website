import { Activity } from "./types";

/**
 * Public Activities Registry.
 *
 * Populated strictly upon receipt of verified, authenticated records of
 * institutional programs, community services, and public welfare initiatives.
 *
 * Maintained as an empty repository until verified records are supplied.
 */
export const activities: Activity[] = [];
